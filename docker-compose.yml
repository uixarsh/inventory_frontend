version: '3.8'

services:
  frontend:
    build:
      context: .
      args:
        # Default to localhost:8080 if not provided in an env file
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080}
    ports:
      - "5173:80"
    restart: unless-stopped
